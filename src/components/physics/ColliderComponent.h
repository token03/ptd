#pragma once

#include "core/Component.h"

// TODO: Implement collision detection logic (e.g., shape, size, layers)
class ColliderComponent : public Component {
 public:
  // Example: A circular collider for range detection or hit detection
  // float radius = 16.0f;
};
